# Core deterministic PDF pipeline, local UI, and lightweight audio plumbing.
# Python 3.12; use -c constraints.txt for the validated dependency snapshot.
flask==3.1.3
pymupdf==1.27.2.2
pdfplumber==0.11.9
num2words==0.5.14
requests==2.32.5
python-dotenv==1.2.1
pydub==0.25.1
